name: minigpu_platform_interface
description: The platform interface for minigpu. A library to brings multiplatform GPU compute to Dart and Flutter using GPU.cpp and Google Dawn.
version: 1.4.7
repository: https://github.com/PracticalXR/minigpu/tree/main/minigpu_platform_interface

environment:
  sdk: ^3.11.4
  flutter: ">=1.20.0"

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8
  minigpu_ffi: 1.4.7
  minigpu_web: 1.4.7

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0

platforms:
  windows:
  android:
  linux:
  ios:
  macos:
  web:
